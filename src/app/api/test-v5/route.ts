import { NextResponse } from 'next/server'; export const dynamic = 'force-dynamic'; export async function GET(req: Request) { try { const { searchParams } = new URL(req.url); const date = searchParams.get('date') || '2026-06-30'; const res = await fetch(https://api.belleforet.com/api/v5/dashboard/revenue-summary?date=, { headers: { 'Authorization': 'Bearer ' + (process.env.M2M_API_TOKEN || 'belleforet-m2m-secret') } }); const data = await res.json(); return NextResponse.json(data); } catch(e:any) { return NextResponse.json({error: e.message}); } }
